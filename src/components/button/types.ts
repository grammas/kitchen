import { Color } from '../../themes/colors';

export type ButtonType = 'submit' | 'reset' | 'button';

export type Color = 'default' | Color;

export type Element = 'a' | 'button' | 'Link';

export type Padding = 'none' | 's' | 'm' | 'l';

export type Shape = 'square' | 'circle' | 'rounded';

export type Variant = 'text' | 'ghost' | 'raised' | 'floating';
